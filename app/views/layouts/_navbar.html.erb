<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <%= link_to 'Post App', root_path, class: 'navbar-brand' %>
      <ul class="navbar-nav clearfix">
        <% if !@current_user.nil? %>
        <span class='align-middle' style='color: white;'>
          <i><small>You are logged in as <%= @current_user.email %></small></i>
        </span>
        <li class="nav-item">
          <%= button_to '<i class="bi bi-plus-square"></i> New Post'.html_safe, new_post_path, method: :get, class: 'btn btn-primary', style: 'margin-left: 0.25rem;' %>
        </li>
        <li class="nav-item">
          <%= button_to '<i class="bi bi-box-arrow-right"></i> Logout'.html_safe, logout_path, method: :post, class: 'btn btn-primary', style: 'margin-left: 0.25rem;' %>
        </li>
        <% else %>
          <li class="nav-item">
            <%= button_to 'Login', new_login_path, method: :get, class: 'btn btn-primary', style: 'margin-left: 0.25rem;' %>
          </li>
          <li class="nav-item">
            <%= button_to 'Register', new_registration_path, method: :get, class: 'btn btn-primary', style: 'margin-left: 0.25rem;' %>
          </li>
        <% end %>
      </ul>
  </div>
</nav>